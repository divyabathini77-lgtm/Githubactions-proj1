name: First-workflow
on:
    push:
        branches:
            - main
jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
         - name: first step
           run: echo "this is my first workflow"
         - name: print good bye
           run: |
            echo "hello"
            echo "good bye"